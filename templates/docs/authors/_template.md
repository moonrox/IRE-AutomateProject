---
# ─────────────────────────────────────────────────────────────────────────────
# IRE Author Writing Profile
# Copy this file to docs/authors/{your-intel-username}.md and fill it in.
# This file is read by AI tools when drafting documents on your behalf.
# ─────────────────────────────────────────────────────────────────────────────
author_profile:
  username: ""              # your Intel username, e.g. jmonroe
  display_name: ""          # your full name, e.g. John Monroe
  role: ""                  # your role, e.g. Product Owner / AI-Enabled Engineer
  team: "IRE — Intel Enterprise Service Platform"

  writing_style:
    # One of: formal | semi-formal | direct | conversational
    tone: direct

    # How do you structure your writing?
    # Examples:
    #   "lead-with-outcome, follow-with-evidence"
    #   "context first, then options, then recommendation"
    #   "numbered steps for procedures, prose for analysis"
    structure: "lead-with-outcome, follow-with-evidence"

    # How long are your typical communications?
    # Examples: concise | thorough | depends-on-audience
    length: concise

    # Words or phrases you prefer. AI uses these when drafting for you.
    # Format: "prefer X over Y"
    vocabulary:
      - ""    # e.g. prefer "surfaced" over "found"
      - ""    # e.g. prefer "aligned" over "agreed"
      - ""    # e.g. prefer "validated" over "confirmed"

    # Words or phrases you avoid. AI will not use these when drafting for you.
    avoid:
      - ""    # e.g. "synergy", "leverage", "circle back"

  ai_continuation:
    # Instructions for AI when drafting a new document on your behalf
    when_drafting: >
      Match the tone and structure above. Use IRE area vocabulary.
      Lead with the outcome or recommendation. Avoid filler transitions.
      Write as if the reader is technically proficient but time-constrained.

    # Instructions for AI when continuing or extending an existing document
    when_continuing: >
      Read the existing document fully before adding content.
      Match the existing section style. Do not introduce new formatting patterns.
      Flag any content that conflicts with the author's stated position.

    # A short HTML comment appended to AI-drafted sections for attribution.
    # Recommended format: <!-- {username} + AI -->
    attribution_tag: "<!-- {username} + AI -->"
---

# {Display Name} — Writing Notes

<!-- Optional: add any free-form notes about your communication style,
     specific audiences you write for, or context the AI should know.
     This section is read by AI tools as supplemental guidance. -->
